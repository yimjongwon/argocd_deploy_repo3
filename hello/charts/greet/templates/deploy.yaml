apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-greet
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: greet
  template:
    metadata:
      labels:
        app: greet
    spec:
      containers:
        - name: greet
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8000